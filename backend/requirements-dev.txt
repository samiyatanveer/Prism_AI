# ============================================================
# Development-only dependencies (not installed in Docker prod image)
# ============================================================
-r requirements.txt

pytest==8.3.4
pytest-asyncio==0.24.0
pytest-mock==3.14.0
httpx==0.28.1      # async test client for FastAPI
black==24.10.0
ruff==0.8.6
