# ============================================================
# Backend .env.example — copy to .env and fill real values
# Never commit .env
# ============================================================

DATABASE_URL=postgresql+asyncpg://prism_user:change_me@localhost:5432/prism_ai
REDIS_URL=redis://localhost:6379/0

GROQ_API_KEY=your_groq_api_key_here
GROQ_MODEL=llama-3.1-8b-instant
# Generate: openssl rand -hex 32
SECRET_KEY=95852a66bddaa3ba6d64062f0e996f6784ec4705eeee0e5e06de8430551c82d4
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Generate: python -c "import base64,os;print(base64.b64encode(os.urandom(32)).decode())"
ENCRYPTION_KEY=replace_with_base64_encoded_32_byte_key

CORS_ORIGINS=http://localhost:3000
APP_ENV=development
LOG_LEVEL=INFO
